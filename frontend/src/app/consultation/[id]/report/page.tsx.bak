'use client';

import dynamic from 'next/dynamic';

const ConsultationReportPage = dynamic(() => import('@/pages/ConsultationReport'), { ssr: false });

export default function Page() {
  return <ConsultationReportPage />;
}
