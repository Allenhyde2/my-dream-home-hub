"use client";

import nextDynamic from 'next/dynamic';

const OnboardingPage = nextDynamic(() => import('@/pages/Onboarding'), { ssr: false });

export const dynamic = 'force-dynamic';

export default function Page() {
  return <OnboardingPage />;
}
