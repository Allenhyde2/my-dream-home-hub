"use client";

import dynamic from 'next/dynamic';

const OnboardingPage = dynamic(() => import('@/pages/Onboarding'), { ssr: false });

export const dynamic = 'force-dynamic';

export default function Page() {
  return <OnboardingPage />;
}
