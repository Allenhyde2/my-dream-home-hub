"use client"
'use client';

import dynamic from 'next/dynamic';

const MyReservationsPage = dynamic(() => import('@/pages/MyReservations'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <MyReservationsPage />;
}
