"use client"
'use client';

import dynamic from 'next/dynamic';

const HomePage = dynamic(() => import('@/pages/Index'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <HomePage />;
}
