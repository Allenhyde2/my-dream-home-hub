"use client"
'use client';

import nextDynamic from 'next/dynamic';

const LoginPage = nextDynamic(() => import('@/pages/Login'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
    return <LoginPage />;
}
