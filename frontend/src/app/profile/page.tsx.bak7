"use client"
'use client';

import nextDynamic from 'next/dynamic';

const ProfilePage = nextDynamic(() => import('@/pages/Profile'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <ProfilePage />;
}
