"use client"
'use client';

import dynamic from 'next/dynamic';

const CourseDetailPage = dynamic(() => import('@/pages/CourseDetail'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <CourseDetailPage />;
}
