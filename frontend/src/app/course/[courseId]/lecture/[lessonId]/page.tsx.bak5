"use client"
'use client';

import dynamic from 'next/dynamic';

const CourseLecturePage = dynamic(() => import('@/pages/CourseLecture'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <CourseLecturePage />;
}
