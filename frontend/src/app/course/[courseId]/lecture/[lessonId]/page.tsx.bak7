"use client"
'use client';

import nextDynamic from 'next/dynamic';

const CourseLecturePage = nextDynamic(() => import('@/pages/CourseLecture'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <CourseLecturePage />;
}
