"use client"
'use client';

import nextDynamic from 'next/dynamic';

const CreatorProfilePage = nextDynamic(() => import('@/pages/CreatorProfile'), { ssr: false });

export const dynamic = 'force-dynamic';
export default function Page() {
  return <CreatorProfilePage />;
}
